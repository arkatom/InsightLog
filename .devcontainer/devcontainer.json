{
  "name": "InsightLog",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "forwardPorts": [5173],
  "remoteEnv": {
    "PLAYWRIGHT_BROWSERS_PATH": "${containerWorkspaceFolder}/.playwright-browsers",
    "GH_CONFIG_DIR": "${containerWorkspaceFolder}/.gh-config",
    "NPM_CONFIG_CACHE": "${containerWorkspaceFolder}/.npm-cache"
  },
  "postCreateCommand": "npm install && npx playwright install --with-deps chromium",
  "postStartCommand": "[ -n \"$GITHUB_TOKEN\" ] && echo \"$GITHUB_TOKEN\" | gh auth login --with-token --hostname github.com --git-protocol https 2>/dev/null; gh auth setup-git 2>/dev/null || true",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss"
      ]
    }
  }
}
